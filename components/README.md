# Deprecated Root Components

This directory is a deprecated mirror of shared UI code.

Active component source of truth:
- `frontend/components/`

Do not add or update components here unless you are intentionally maintaining a temporary compatibility shim.
