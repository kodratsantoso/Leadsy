# Root Types Note

The live UI is served from `frontend/`.

If a type is needed by the running frontend, prefer placing it in:
- `frontend/types/`

Root-level `types/` remains only for compatibility with the deprecated root Next.js tree.
