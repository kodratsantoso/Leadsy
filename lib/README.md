# Deprecated Root Lib Tree

This directory is a deprecated mirror of frontend helper code.

Active source of truth:
- `frontend/lib/`

New frontend utilities, API clients, permission helpers, and hooks should be added under `frontend/lib/`.
