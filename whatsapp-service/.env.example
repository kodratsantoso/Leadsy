PORT=3002
LARAVEL_WEBHOOK_URL=http://127.0.0.1:8000/api/webhooks/whatsapp
# Shared secret sent as the X-Webhook-Secret header on every webhook call — must
# match WHATSAPP_WEBHOOK_SECRET in backend/.env exactly. Generate with: openssl rand -hex 32
WEBHOOK_SECRET=
