# Deprecated Root Store Tree

This directory is a deprecated mirror of the active frontend state layer.

Active source of truth:
- `frontend/store/`

Any auth/session/UI store changes should be made in `frontend/store/`.
